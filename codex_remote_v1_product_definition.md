# Codex Remote V1 产品定义

## 1. 文档目的

本文定义 Codex Remote V1 的产品目标、核心概念、用户流程、功能范围与明确非目标。

本文只描述产品行为，不规定 Host 的进程管理、进程间通信、数据库、日志格式或客户端—服务端协议等实现细节。相关技术设计见 `codex_remote_host_architecture_v1.md`、`codex_remote_cs_protocol_v1.md` 和 `codex_remote_audit_v1.md`。

当前仓库只交付 Linux Host 个人 Demo；`docs/decision_baseline.md` 是冲突表述的覆盖基线。现有功能默认保留，功能正确与可排错优先，不把生产安全、合规审计或高吞吐作为 V1 验收范围。

## 2. 产品定位

Codex Remote 是 Codex 的远程结构化客户端。它让用户从远程设备查看和控制运行在自己开发机上的 Codex，同时继续使用开发机本地的代码、工具、凭据和执行环境。

V1 面向个人或小范围自用场景，重点是：

- 以结构化会话和活动呈现 Codex，而不是远程控制终端或解析 TUI；
- 能创建新的 Codex session；
- 能发现、导入并继续开发机上已有的 Codex session；
- 让 Codex 的运行过程可持续查看和控制；
- 通过私有网络访问开发机，不要求业务数据经过产品方的中心中继服务。

## 3. V1 产品范围

### 3.1 平台范围

V1 Host 仅支持 Linux。

macOS 和 Windows Host 不属于 V1 范围。客户端形态及具体平台可独立演进，不改变本条对 Host 的范围限定。

### 3.2 使用规模

V1 按 Demo 和个人自用场景设计，不以多租户、大量并发用户、企业级权限治理或大规模集中运维为目标。

### 3.3 Host 网络范围

V1 Host 自身加入用户的公共 Tailscale Tailnet，不要求 Linux 宿主机预先安装或运行系统级 Tailscale 服务。Host 的 Codex 业务数据不经过 Codex Remote 自建中心 relay，也不通过公网 Funnel 暴露。

客户端如何加入或访问 Tailnet 不属于当前服务器端范围。

### 3.4 核心对象

产品中：

> 一个 Codex = 一个 Codex session。

每个 Codex 绑定一个工作目录和一个底层 Codex session。用户看到的对话、运行状态、命令、文件修改、审批和后续交互都归属于该 Codex。

“继续已有 session”不会复制成另一个 session；同一个已导入 session 应复用同一个产品记录，避免重复展示和重复管理。

## 4. 核心用户流程

### 4.1 选择已有目录

用户选择开发机上一个已存在的目录后，系统应先列出与该目录关联、可继续的本地 Codex sessions，而不是直接创建新 session。

用户可以：

1. 选择一个已有 session，导入到 Codex Remote 并继续；或
2. 在该目录中新建一个 session。

列表应提供足够信息帮助用户识别 session，例如标题或内容预览、最近活动时间、来源和状态。产品可以隐藏不适合直接交互的内部或子 agent session。

### 4.2 输入不存在的新目录

用户输入一个不存在的目录时，系统应自动创建该目录，并在其中创建一个新 Codex session。

该流程不要求用户预先登录开发机手动建目录。若目录无法创建，客户端应给出明确错误，且不得表现为 session 已创建成功。

### 4.3 导入并继续已有 session

Codex Remote 应支持发现和导入由本地 Codex CLI、编辑器集成或其他兼容入口创建的已有 session，只要底层 Codex 能识别并继续该 session。

“导入”表示 Codex Remote 开始管理和展示该 session：

- 不改变其原有身份；
- 不复制或重写其原始对话；
- 保留其既有历史，并允许继续追加新的交互；
- 能区分由 Codex Remote 创建的 session 与从本地导入的 session。

### 4.4 创建新 session

用户可以在任意有效工作目录中创建新 Codex。创建成功后，该 Codex 应出现在客户端列表中，并可以立即进行结构化交互。

### 4.5 远程交互

V1 的核心交互至少包括：

- 查看 session 历史和当前状态；
- 发送新消息并接收流式结果；
- 查看命令执行、工具活动和文件修改；
- 处理 Codex 发起的审批或用户输入请求；
- 中断当前运行；
- 在断线后重新进入并恢复到可理解的会话状态。

具体字段、事件封装和重连协议不在本文中定义。

### 4.6 可审计与可排查

V1 必须能在问题发生后区分故障位于 Client、网络/连接、Host、CodexAdapter 还是 app-server。Client 与 Host 都应保留可导出的运行证据，记录应同时适合人工阅读和机器对账；不能只依赖临时 console、最终对话或单侧日志。

## 5. 目录与 session 行为规则

| 用户选择 | 系统行为 | 用户下一步 |
|---|---|---|
| 已存在目录，且有本地 sessions | 展示该目录的已有 sessions | 继续一个已有 session，或新建 |
| 已存在目录，但无本地 session | 展示空状态并提供新建入口 | 新建 session |
| 不存在目录 | 自动创建目录 | 自动新建 session |
| 已导入过的 session | 复用已有 Codex 产品记录 | 直接打开并继续 |
| 无法访问或创建目录 | 明确报告失败 | 修改目录或处理权限问题 |

目录匹配应以开发机上的规范化路径为准，避免同一路径因表现形式不同而产生重复工作区或错误关联。

## 6. session 生命周期与并发边界

V1 支持继续已经持久化的本地 Codex session。

计划重启只允许发生在没有 active turn 时。重启后产品必须保持同一 session/历史的可见性、稳定 Codex 映射和继续能力；V1 不承诺 active turn 跨进程热恢复，也不重放结果未知的副作用。

V1 不承诺 Codex Remote 与一个仍在运行的本地 Codex TUI 对同一个 live session 进行实时双控。具体而言，不保证：

- 两个前端实时收到完全一致的增量事件；
- 两端可同时发送输入而不发生竞争；
- 审批、打断和状态切换能在两端一致同步。

产品应避免将这种场景宣传为已支持能力。必要时可以提示用户先停止本地 TUI 对该 session 的实时操作，再从 Codex Remote 继续。未来若 Codex 官方提供可靠的多前端订阅和控制语义，可再扩展此能力。

## 7. V1 成功标准

V1 Demo 达到以下结果即可视为核心闭环成立：

1. Linux 开发机可以运行 Host，并被远程客户端发现和访问；
2. 用户可选择一个已有目录并看到该目录的可继续 sessions；
3. 用户可继续已有 session，且历史可见、后续消息追加到同一 session；
4. 用户可在已有目录中创建新 session；
5. 用户输入不存在的目录时，系统能创建目录并启动新 session；
6. 远程端可查看结构化活动、发送消息、处理中断与审批；
7. 断线或 Host 重启后，已持久化 session 仍可重新发现和继续；
8. 出现问题时，开发者可以通过 Client/Host 双侧记录还原关键链路，并初步界定问题所在组件。

第 8 项是产品质量要求；其具体记录层次和存储方式属于架构设计。

## 8. V1 非目标

V1 不包含或不承诺：

- macOS 或 Windows Host；
- 多租户、企业组织、复杂 RBAC 或公网匿名访问；
- 大规模用户和高并发部署；
- 替代 Codex 的 agent loop、工具执行、sandbox 或 session 存储；
- 通过 PTY 解析或远程控制 Codex TUI；
- 与运行中的本地 TUI 对同一 live session 的可靠实时双控；
- 在产品定义中展开客户端—服务端协议或内部存储实现。

## 9. 待定事项

- 客户端形态与 V1 首发平台；
- 多客户端同时连接同一 Codex 时的观察者和控制者规则；
- live session 占用检测及对应提示策略；
- 导入 session 时各来源类型的具体展示和过滤规则。
